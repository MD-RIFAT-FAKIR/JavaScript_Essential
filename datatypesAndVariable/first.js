/*variable allows to store value so,
 that we can leter or wheater needed in programm */

/*datatypes tell the type of value stored in variable*/

let name = "Rifat";//string datatypes
let age = 25;//intiger datatypes
let isMarrid = false;//boolean datatypes retuns 0 or 1 whre tree = 1 and false = 0
let car;//undifined, we just decraled but not assigned any value to the car variable
let jobPosition = null;//willingly assigned null,for assing value in future
const pi = 3.1416//const keyword used to store only static value,that can not be change 

console.log(name);
console.log(age);
console.log(isMarrid);
console.log(car);
console.log(jobPosition);
console.log(pi);

//typeof is an operator the gives types of data stored in variable
console.log(typeof name);
console.log(typeof age);


/*

//js datatypes can be divided into 2 categories

#1(pimitive){
    string
    number
    boolean
    undifined
    null
    symbol
    bigint

    //all stored in stak memory
}

#2(reference){
    object
    array
    function

    //all stored in heap memory
}

*/